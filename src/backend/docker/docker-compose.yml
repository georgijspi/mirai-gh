services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
    volumes:
      - mongodb_data:/data/db

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: always
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    # Optional NVIDIA GPU support
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    # Use runtime configuration if nvidia-container-runtime is installed
    # runtime: nvidia

volumes:
  mongodb_data:
  ollama_data: 